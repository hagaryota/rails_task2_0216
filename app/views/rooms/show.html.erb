<h2>ルーム詳細</h2>
 
<table>
  <tr>
    <td>
      <% if @room.image.attached? %>
        <%= image_tag @room.image, class: "room_image" %>
      <% else %>
        <%= image_tag "https://presite-potepancamp-rails-02.herokuapp.com/assets/room01.jpg", class: "room_image" %>
      <% end %></td>
  </tr>
  <tr>
    <th>ルーム名</th>
    <td><%= @room.name %></td>
  </tr>
  <tr>
    <th>住所</th>
    <td><%= @room.address %></td>
  </tr>
  <tr>
    <th>ルーム紹介</th>
    <td><%= @room.introduce %></td>
  </tr>
  <tr>
    <th>料金</th>
    <td><%= @room.price %> </td>
  </tr>
</table>

<div>
   <ul>
    <li><%= link_to "登録済みルーム一覧に戻る", :rooms %></li>
      <li><%= link_to "ルームを編集する", [:edit, @room] %></li>
      <li><%= button_to "ルームを削除する", @room, method: :delete, data: { confirm: "本当に削除しますか？" } %></li>
   </ul>
</div> 